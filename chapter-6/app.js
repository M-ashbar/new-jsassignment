//1. Create a variable count with an initial value of 8.
//Use the post-increment operator to increment count, then log the result to the console.

var count = 8;
console.log( count++)

//2. Initialize a variable x with a value of 15. Use the pre-decrement operator to decrement x,
 //then display the updated value.

 var x =15;
 console.log( --x )

 //3.Declare a variable num and set it to 25. 
 //Use the post-decrement operator to decrement num, then print the result.
 var num = 25;
 console.log(num--)

 //4.Start with a variable value set to 10. Apply the pre-increment operator to value,
 // and then output the new value.
 var value = 10;
 console.log(++value)
 
 //5.Set a variable index to 5.
  //Apply the post-increment operator to index and display its value.
  var index =5;
  index++;
  console.log(index)
 
 
 //6.Define a variable quantity and assign it the value 12. Use the 
   //  pre-decrement operator on quantity,and then show its updated value.
 var quantity =12;
 --quantity;
 console.log(quantity)

   //7.Initialize a variable counter with a value of 18. 
  // Use the post-increment operator on counter and log the result.
  var counter = 18;
  console.log(counter++ )

  //8.Create a variable score set to 30. 
  //Use the post-decrement operator on score, then print the result.
  var score = 30;
  console.log(score-- )


  //9. Start with a variable value set to 20. 
  //Apply the pre-increment operator to value, and then output the new value.
  var value=20;
  console.log( ++value )

  //10. Declare a variable counter and set it to 8.
   //Apply the post-decrement operator to counter, then display the result.
   var counter= 8;
   counter--;
   console.log( counter)
